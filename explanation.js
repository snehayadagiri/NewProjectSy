/**
->when you create a index.html or any .html file , type doc and
  click enter it will give a whole template that you need not to write manually
->command + ? gives a comment line

-> option (alt) + z -> will force the code to look visible. for example https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.1/css/all.min.css
we can see the above code in index.hetml file type above command and hit enter it will
put the code in visible format









 */